export const SEARCH_BAR_I18N = {
  placeholder: {
    key: 'searchBar.placeholder',
    fallback: 'Search collection… (catalogue number or label)',
  },
  ariaLabel: { key: 'searchBar.ariaLabel', fallback: 'Search collection' },
  noMatches: { key: 'searchBar.noMatches', fallback: 'No matches' },
  noCurrentLocation: { key: 'searchBar.noCurrentLocation', fallback: 'No current location' },
} as const;
