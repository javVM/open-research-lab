export const LOCATION_TREE_I18N = {
  ariaLabel: { key: 'locationTree.ariaLabel', fallback: 'Physical location hierarchy' },
  collapse: { key: 'locationTree.toggle.collapse', fallback: 'Collapse {name}' },
  expand: { key: 'locationTree.toggle.expand', fallback: 'Expand {name}' },
} as const;
